@using System.Linq.Expressions;
@using Tyne.Blazor.Filtering;
@namespace Tyne.Blazor.Filtering.Controllers
@typeparam TRequest
@typeparam TValue
@inherits TyneMinMaxFilterController<TRequest, TValue>

@if (ChildContent is not null)
{
    @ChildContent(this)
}
