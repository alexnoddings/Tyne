@namespace Tyne.Blazor.Filtering
@typeparam TRequest
@typeparam TValue
@inherits TyneFilterControllerBase<TRequest, TValue>
@implements ITyneCustomFilterContext<TValue>

@if (ChildContent is not null)
{
    @ChildContent(this)
}
