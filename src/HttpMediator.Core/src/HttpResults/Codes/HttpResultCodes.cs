namespace Tyne.HttpMediator;

/// <summary>
///     Marker class for HTTP result extension methods (e.g. <see cref="HttpResultCodesExtensions.OK{T}(HttpResultCodes, in T)"/>).
/// </summary>
public sealed class HttpResultCodes
{
    internal HttpResultCodes() { }
}
