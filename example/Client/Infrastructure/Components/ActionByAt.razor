@using Humanizer;

<RelativeDateTime DateTimeUtc="AtUtc" />
by
@if (ByName is null)
{
    <span class="mud-text-disabled">
        (deleted user)
    </span>
}
else
{
    <span>
        @ByName
    </span>
}
