@page "/systems/filtering/synchronisation"
@layout FilteringExampleLayout
@attribute [SystemTitle("Filter synchronisation")]

<DocsTextBlock>
    All filter controllers are synchronised with other controllers attached to the same filter value.
    Whenever one controller updates the value, the rest will re-render with the new value.
</DocsTextBlock>

<SourceCode T="SyncFilterExampleContent" SourceType="SourceCodeType.Component" Name="Filter context">
    <SyncFilterExampleContent />
</SourceCode>
